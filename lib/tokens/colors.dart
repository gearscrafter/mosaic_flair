import 'dart:ui';

const Color primary = Color(
    0xFFA67C72); //Un tono marrón rojizo para botones principales, encabezados, y elementos de énfasis.
const Color background = Color(
    0xFFF2F2F2); // Un color gris muy claro, ideal para fondos generales y crear espacios limpios.
const Color secondaryBackground = Color(
    0xFFD9D9D9); // Un gris claro, útil para secciones de fondo alternativo, tarjetas, o paneles.
const Color neutral = Color(
    0xFFA6A6A6); // Un gris mediano para texto secundario, íconos, bordes o elementos de menor jerarquía.

const Color accent1 = Color(
    0xFFFFB347); // Un tono naranja cálido para botones de llamada a la acción, promociones y elementos interactivos.
const Color accent2 = Color(
    0xFF79C7C5); // Un color verde azulado para elementos de resaltado y acento en componentes interactivos.

const Color success = Color(
    0xFF5BBD72); // Un verde para estados de éxito, confirmaciones, y mensajes positivos.
const Color warning = Color(
    0xFFFF7F50); //Un tono coral para advertencias, alertas, o notificaciones importantes.
const Color error = Color(
    0xFFE57373); // Un rojo claro para mensajes de error y validación negativa.

const Color textPrimary = Color(
    0xFF333333); // Un gris oscuro casi negro para el texto principal que asegura buena legibilidad.
